spring:
  datasource:
    url: jdbc:mysql://localhost:3307/uams?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: hzy2005
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
