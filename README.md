RAR:
问题： dashboard/index.vue 位于 src/views/dashboard/index.vue ，文件层级为 2 级，相对路径应使用 ../../api/dashboard 。

Issue: dashboard/index.vue is located at src/views/dashboard/index.vue with a depth of 2 levels, therefore the correct relative import path is ../../api/dashboard .
